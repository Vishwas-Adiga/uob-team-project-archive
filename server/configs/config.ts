export const Config = {
  PORT: 5000,
  // Port exposed by Vite when running in development mode
  VITE_PORT: 5173,
  // Regex that matches emails issued by the host organisation
  ORG_EMAIL_REGEX: /^[\w+.?]+@(student\.)?bham\.ac\.uk$/g,
};
